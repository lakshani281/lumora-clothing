export interface Category {
  id: string;
  title: string;
  stylesCount: string;
  imageUrl: string;
}
export interface Category {
  id: string;
  title: string;
  stylesCount: string;
  imageUrl: string;
}

export interface Product {
  id: string;
  name: string;
  rating: number;
  reviewsCount: number;
  price: string;
  imageUrl: string;
  colors: string[];
}
export interface Category {
  id: string;
  title: string;
  stylesCount: string;
  imageUrl: string;
}

export interface Product {
  id: string;
  name: string;
  rating: number;
  reviewsCount: number;
  price: string;
  imageUrl: string;
  colors: string[];
}

export interface PromiseFeature {
  id: string;
  icon: string;
  title: string;
  description: string;
}